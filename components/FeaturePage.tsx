import { ProductGrid, SectionTitle } from './Sections';
import { SiteChrome } from './SiteChrome';

export function FeaturePage({ eyebrow, title, text, features, cta = 'Continue exploring' }: { eyebrow: string; title: string; text: string; features: string[]; cta?: string }) {
  return <SiteChrome><section className="premium-gradient px-6 pb-20 pt-36"><SectionTitle eyebrow={eyebrow} title={title} text={text} /><div className="mx-auto grid max-w-7xl gap-6 md:grid-cols-3">{features.map((feature, index) => <article data-reveal key={feature} className="card-lift rounded-boutique bg-white/80 p-8 shadow-sm backdrop-blur"><div className="mb-6 grid h-52 place-items-center rounded-3xl bg-gradient-to-br from-rose/45 via-white to-sage/35"><span className="font-heading text-6xl text-gold">0{index + 1}</span></div><h2 className="font-heading text-4xl">{feature}</h2><p className="mt-3 leading-7 text-charcoal/65">Built with soft reveal motion, premium spacing, responsive cards, accessible copy, and production-ready placeholders for real Petal Drift data.</p></article>)}</div><div className="mx-auto mt-12 max-w-7xl rounded-[3rem] bg-white p-8 shadow-boutique"><h3 className="font-heading text-5xl">{cta}</h3><p className="mt-3 text-charcoal/65">This page is structured so the admin can replace every title, image, product tag, gallery item, and story block after the data layer is connected.</p></div></section><ProductGrid /></SiteChrome>;
}
